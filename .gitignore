node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
.env
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts
/prisma/*.db
/exports

# Local artefacts
/ui-shots

# Job number snapshots written by scripts/renumber-job-nos.mjs. They name
# live rows and are a rollback record, not source.
/backups
/prisma/migrations/**/shadow.db
*.local.md

# Deployment credentials (never committed)
.deploy.env

# Deployment artefact, rebuilt on every push.
deploy/.artifact.tgz
